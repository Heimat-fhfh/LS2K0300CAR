现在需要修改 src/main_impl.cpp 中的 ProcessAlgo_AcrossTrackTask内容，因为我已经不在使用黑块检测的方式了
将需要多次调整的参数添加到config json文件中
判断的主要依据是 Data_Path 中的数据

首先是十字的四个阶段的判定，然后是补线策略，整体来看十字内的总帧数不可以超过指定值，阶段必须按顺序进行，不能跳过

当 Judge 循环判定赛道为十字循环后，十字状态被初始化，直接进入十字的第一个阶段 ACROSS_PREPARE
如果是 左/右 十字，则当 右/左 侧边缘区域点数量小于 30 时进入第二个阶段 ACROSS
当 右/左 侧出现一个跳变点，进入第三个阶段 ACROSS_OUT
当 右/左 侧边缘区域点数量大于 70，进入第四个阶段 ACROSS_OUT_2
当 右/左 两侧任意一边 边缘区域点数量小于 20 则退出十字循环

暂时无需补线函数


